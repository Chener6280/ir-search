# 公众号素材首批验收

验收日期：2026-09-16。仅记录当前账号、时点和有界样本，不表示全账号、全历史或微信全网覆盖。使用用户已授权的本机极致了 Key；未记录 Key、账户余额或完整私有公众号清单。

## 实际结果

| 路径 | 结果 | 实际边界 |
|---|---|---|
| 供应商接口核对 | 账号查询、`post_history`、`article_html` 均成功 | 使用当前公布的 HTTPS POST 接口；历史列表用返回的 offset，不沿用旧客户端猜测的 page/post_detail 参数 |
| SDK `search_materials` | 3 个配置公众号；收到 32 条、实际检查 18 条；命中 7 组素材、15 条引用 | 查询发布日期 2026-09-01 至 2026-09-16；每账号检查 6 条，三个账号均有更多历史记录未检查 |
| 正文读取 | 3 篇提取文字，长度分别为 17,801 / 123 / 550 字符；另 4 组仅摘要 | 此次微信原站未返回可用文章正文，三篇均由极致了补充读取；已标明正文提供方和 fallback |
| 显式 `retrieve` | 1 篇正文，返回 12,000 字符与 10 条引用 | 按请求长度截断；保留供应商文本、截断和原站失败警告 |
| 真实 MCP `search_materials` | 新 `wechat_accounts` 参数生效；1 组正文、3 条引用 | 单账号、候选预算 3、正文预算 1；正文 17,801 字符，由极致了提供 |
| 引用一致性 | 上述素材及 retrieve 引用均逐一核对文本和字符位置 | 引用绑定实际返回文字；不推定业务期间、不输出未经提取的图表内容 |

三个搜索账号均成功，结果 `status=partial`、`complete=false`，并含候选预算与非穷尽检索诊断。短正文样本属于带有图文内容的文章引言，不能把取得的少量文字认定为整篇投研资料已解析；当前明确标注图片与媒体未转写，并对短提取文字增加警告。

原站成功读取、原站失败转供应商、身份错配、认证失败、余额不足、错误分页、未知日期、链接清洗、取消/超时、候选和读取上限均有合成测试。**本轮真实样本仅验收到供应商补充读取成功，未宣称微信原站在当前环境下稳定可读。**

## 工程验证

- `python3 -m pytest -q`：**1114 passed, 12 skipped**。另有 5 条原有 PyMuPDF/SWIG 弃用警告。
- Python 3.12 的安装包/MCP/公众号/正文/素材/公共传输相关测试：**149 passed**。
- 构建 wheel、在无源码路径的无关目录安装运行；验证 SDK、MCP、来源状态、资源加载，以及新公众号来源和模拟正文读取。
- wheel 私有文件排除测试包含 env、旧账号文件和新 `.local/wechat_accounts.json`；运行实现不引用桌面 skills 或 checkout-only 工具。
- 对 288 个代码、测试、文档及示例文件检查实际凭证值，未发现匹配。真实响应只保存在本机被忽略的 `.local/` 中。
- `git diff --check` 通过。

## 本机配置与后续边界

现有 53 个极致了账号配置迁入 `.local/wechat_accounts.json`，不改动桌面参考 skills。Key 归入 `credentials.env`，两者权限均为 `600`。默认每次最多 3 个账号、每账号 2 页；每次调用还受候选、正文和总时间预算约束。

第一批交付为账号池取材与正文引用。账号池外的博查关键词发现、RSS、图片 OCR、持久化索引仍为后续工作；未扩建 `deep_research`。当前 SDK/MCP 配置与字段语义见[公众号素材指南](wechat_material_adapter.md)。

后续的 Crawl4AI 补充读取、私有缓存与减少重复供应商调用验收，见[公众号省钱读取验收](wechat_savings_acceptance.md)。

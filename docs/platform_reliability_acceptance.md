# 来源可靠性改进验收（2026-09-18）

本轮完成来源诊断、续取修复和可选私有运行摘要，并在独立安装包中验证 SDK 与 MCP。功能与参考取舍见 [使用说明](platform_reliability.md)。本轮没有重新验收所有数据供应商，也没有为 `deep_research` 增加能力。

## 回归与独立部署

| 检查 | 结果 |
| --- | --- |
| `python3 -m pytest -q` | **1835 passed，15 skipped**；5 条既有 PDF/SWIG 警告 |
| 支持现代打包工具的隔离环境运行 `tests/test_standalone_package.py` | **3 passed** |
| 安装 wheel 后，从与源码无关的目录调用 SDK | 模块来自独立安装目录；新诊断、运行记录及续取接口可用 |
| 已安装包注册真实 FastMCP 工具 | 原有 **12 个工具**均可注册；`source_health` 和 `search_materials` 新参数可调用 |
| wheel 内容检查 | 无 credentials.env、Cookie、私有 `.local` 资料；未发现已知凭证值 |
| 文件格式与变更检查 | `git diff --check` 通过 |

独立实测使用的 wheel SHA-256：`fe247b675012e77e2fbf96d62b12001b69bf3f6dacd8f0ef3de2b30b59cd7dff`。这是验收构建的标识，不表示已经发布到 GitHub 或软件仓库。

新增回归覆盖：可选依赖与必需依赖区分；本地诊断不联网；登录成功不冒充搜索或正文可用；取消与错误脱敏；运行摘要的权限、篡改、符号链接与容量边界；长游标与查询/账户绑定；公众号、IMA、星球中断续取；返回上限截断时拒绝跳页。

## 本地体检

默认体检列出 **19 个来源**，数据源调用次数为 **0**。检查不同 Python 环境时发现依赖不同：基础环境缺少 Wind/JYDB 所需的 PyMySQL 和 Alpha派所需的 Playwright；浏览器环境缺少 PyMySQL 与 AKShare。诊断按实际解释器输出缺失模块和对应安装选项，没有将其描述为远端账号或数据库故障，也没有自动安装依赖。

显式小红书 live 检查成功，状态为 `login_verified`，后端为本机小红书 MCP。探测范围仅为登录状态，`search_live_verified`、`retrieve_live_verified`、`get_data_live_verified` 仍为 false。其他来源暂未实现轻量 live 探测，明确返回 `live_probe_not_implemented`。

## 小红书真实调用

从独立安装包使用既有登录态进行有界查询：关键词“茅台”，发布日期 2026-09-01 至 2026-09-18，每次最多检查 2 个候选，正文读取预算为 0。该范围用于检验接口、游标和记录，不代表平台全量覆盖或正文验收。

| 调用 | 观察结果 |
| --- | --- |
| 首次 SDK 搜索 | 超时，0 组结果；保留 `timeout` 和范围不完整诊断；运行摘要成功保存 |
| 后续真实 MCP 搜索 | 返回 **2 组素材**，运行摘要成功保存 |
| 再次 SDK 搜索 | 返回 **2 组素材**，提供可续查位置 |
| `next_material_request` 构造下一批后调用 SDK | 返回 **1 组素材**，与前一批引用无重复；运行摘要成功保存 |
| 默认 MCP `source_health` | 只检查本地状态，数据源调用次数为 0 |

上述成功结果仍保留 `xhs_search_bounded_not_exhaustive` 和 `bounded_search_not_exhaustive`，没有标记为完整搜索。续查使用已有的短期搜索快照；快照过期后的行为由测试覆盖，本轮没有将过期快照延长为持久游标。

前一次超时和之后成功都作为观察保留：**小红书登录可用，但搜索后端仍会偶发超时**。本轮改进使失败、处理建议和续查边界可见，没有声称修复平台本身或保证持续可用。正文与评论不在本轮 live 查询的验证范围，既有验收见 [小红书验收](xhs_material_acceptance.md)。

## 私有记录与参考资料

原始验收结果保存在本机被 Git 忽略的 `.local/platform-improvements/`，0700 目录、0600 JSON 文件；运行摘要另存其 `runs/` 子目录。完整搜索结果仅用于私有验收，默认运行摘要只保存白名单统计与指纹。检查未发现当前已知凭证值出现在验收输出，也未发现查询明文和笔记引用链接进入运行摘要。摘要不是匿名化承诺或研究证据。

本地参考库共 **39 条**。本轮新增三份固定版本的公开项目参考与 WikiSkill 参考卡片，保留许可证、出处及散列。没有安装这三个项目作为运行依赖，没有复制 MediaCrawler 运行代码，没有自动修改 skills。测试和文档中的故障模式经过审阅，规则变更仍须回归验证。

# FMP 正式 SDK/MCP 验收

验收时间：2026-09-15 05:43:36–05:43:45 UTC。使用用户已填入本机私有 env 的 Key 和用户声明的免费套餐。通过正式包内 adapter 取数，不调用桌面 skills；本轮五次只读 HTTPS 请求，无重试、无购买或升级。

| 检查 | 实际结果 |
|---|---|
| SDK `securities` | AAPL 公司资料 1 条，NASDAQ / USD，ok |
| SDK `prices_daily_basic` | 2026-09-08 至 09-14，共 5 个交易日价格与原始成交计数，partial |
| SDK `financial_statements_standardized` | 2021–2025 财年各三张表，共 15 条；金额币种 USD，partial |
| 财务内部一致性 | 五期资产 = 负债 + 总权益；五期经营现金流 + 带符号资本开支 = 自由现金流 |
| 真实 FastMCP `get_data` | 与 SDK 相同的 15 条财务记录、相同来源抓取时间，显示缓存命中；新增 HTTP 请求为 0 |
| MCP 工具目录 | 保持 11 个工具，通过既有入口调用 FMP |

请求计数：profile 1 次、Light 日线 1 次、年度利润/资产负债/现金流各 1 次，共 5 次。连同此前独立账号探测的 5 次，本对话这两轮验收共 10 次，不代表账户全部用量或剩余额度。

财年结束日分别为 2021-09-25、2022-09-24、2023-09-30、2024-09-28、2025-09-27。未把非自然年财报转换成 12 月 31 日，未转换原币金额，未把供应商净利润替换为国内归母净利润。内部恒等式检查不是 SEC 原件审计。

日线和财务返回 partial 是预期行为：Light 复权和成交计数口径未核验；年度财务读取供应商最近 N 期当前快照，非完整历史、原始/重述版本库或 PIT。范围、时区、版本哈希、跨表对齐和来源层级的诊断均保留。验收只证明此次 AAPL 样本成功，不推定所有股票、国家或 Ultimate 端点可用。

私有详细记录保存在 Git 忽略的 `.local/fmp-adapter-acceptance.json`，权限 0600，仅含请求端点名、计数、日期、币种和诊断，不含 Key、认证 URL 或完整原始报表。该脚本是一次性验收工具，正式安装包不依赖它。

配置及后续扩展说明见 [FMP adapter 指南](fmp_adapter.md)。

工程检查：全项目 `python3 -m pytest -q` 为 823 通过、7 跳过；独立 Python 3.12 环境的 FMP、MCP、独立 wheel 安装检查为 90 通过。覆盖认证/权限/限流错误、不重试、预算、缓存、取消、响应大小、日期/字段/币种/版本、SDK/MCP 路由与安装包独立性。默认环境仍有既有 PDF 依赖弃用警告。对待发布文件扫描本机已配置的 Key/密码/Token，匹配数为零；私有 env 与验收记录均被 Git 忽略。
